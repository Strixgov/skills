# /govern-pr — worked examples

Reference outputs of the change-scoped governance review.

| # | Target | Verdict | What it demonstrates |
|---|--------|---------|----------------------|
| [01](./01-strix-pr-1670-decision-trace.md) | strix-platform PR #1670 (Decision Trace v1 + strategy docs) | **ADDITIVE** | Step 1 scoping doing real work (ignoring 374 already-merged files behind a stale local `main` baseline); an honest ADDITIVE verdict on a change that *touches a decision route but only additively*; invariant-by-invariant impact with the no-authority guard called out. |

Every example is advisory: the review reads the diff and reasons about it; it
never blocks a merge, runs anything, or produces a signed record.
